let cols, rows;
let scale = 20;

function setup() {
  size(600,600,P3D);
  let w = 600;
  let h = 600;
}

function draw() {
  background(0);
  for (let y = 0; y < rows; y++) {
    for (let x = 0; x < cols ; x++) {
     stroke(255);
    }
  }
}